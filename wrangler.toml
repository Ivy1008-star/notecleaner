name = "notecleaner"
compatibility_date = "2025-07-25"
compatibility_flags = ["nodejs_compat"]

# Workers + Assets 统一平台配置
# main 指向 next-on-pages 生成的 Worker 入口
# assets 指向静态文件目录
main = ".vercel/output/_worker.js/index.js"
assets = { directory = ".vercel/output/static", binding = "ASSETS" }